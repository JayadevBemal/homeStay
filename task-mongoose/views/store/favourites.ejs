<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head') %>
</head>
<body>
  <%- include('../partials/nav') %>
  <ul class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
    <% homes.forEach((home)=>{%>
      
      <li class="bg-white rounded-lg shadow-md hover:shadow-xl transition-shadow overflow-hidden">
              <img src="<%= home.homeImage %>" alt="<%= home.houseName %>" class="w-full h-48 object-cover">
              <div class="p-6">
                <h2 class="text-lg font-semibold text-gray-800 mb-3"><%= home.houseName %></h2>
                <div class="space-y-2 text-sm text-gray-600">
                  <p><span class="font-medium">Price:</span> <%= home.pricePerNight %></p>
                  <p><span class="font-medium">Location:</span> <%= home.location %></p>
                  <p><span class="font-medium">Rating:</span> <%= home.rating %></p>
                </div>
                <div class="my-2 flex items-center gap-2">
                  <a href="" class="font-medium bg-lime-500 py-1 px-3 rounded-lg hover:bg-slate-400">Book</a>
                  <a href="/home-list/<%= home.id %>" class="font-medium bg-teal-400 py-1 px-3 rounded-lg hover:bg-slate-400">Detail</a>
                  <form action="/favourites/<%= home._id %>" method="POST">
                  <button type="submit" class="font-medium bg-red-500 py-1 px-3 rounded-lg hover:bg-slate-400">Remove</button>
                  </form>
                </div>
              </div>
            </li>
      
      
   <% }) %>
  </ul>
</body>
</html>